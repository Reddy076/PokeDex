{
  "builds": [
    {
      "src": "PokeDex/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "PokeDex/index.html"
    }
  ]
}